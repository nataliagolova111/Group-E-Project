Every person gets a different function

function 1 - add_item() returns name and quantity
             valid_name(), valid_quantity().

function 2 - save_item() saves info about item in a separate
             items.txt file.

function 3 - view_items() prints a well organized table of
             all the items and their quantity.